# Interleaving Improves Strategy Selection

Interleaving practice problems from different topics during a single study session (rather than blocking all problems of one type together) improves a learner's ability to select the correct strategy on a test, because the learner must practice discriminating between problem types rather than just executing one procedure repeatedly.
